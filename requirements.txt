# Django core
django==5.2
asgiref==3.8.1
sqlparse==0.5.0

# Static files & production
whitenoise==6.6.0
gunicorn==21.2.0
python-decouple==3.8
django-environ==0.11.2

# PostgreSQL (remove if using SQLite)
psycopg2-binary==2.9.9

# Env support
python-dotenv==1.0.1

# Requests if still used for integrations (like chatbot etc)
requests==2.31.0
